# 当前项目发布规则

## Skill 发布前敏感信息检查

在本项目中创建、更新或发布任何 skill 前，必须对 skill 源文件、说明文档、脚本、示例、配置文件和仓库元数据进行敏感信息扫描。

重点检查：

- 公司、客户、项目、私域或内网域名
- 组织名称、品牌名称和内部系统名称
- API Key、Token、Secret、密码、Cookie 和私有凭证
- 本机绝对路径、内部文件路径和服务器地址
- 真实邮箱、电话号码、账号标识和其他个人信息
- 仅适用于特定组织的内部流程或配置

如果发现敏感信息：

1. 必须明确指出文件位置和具体内容类型。
2. 必须给出不泄露真实信息、但仍保持语义清晰的替换建议。
3. 未获得用户明确确认前，不得修改敏感内容并推送发布。
4. 用户确认后，才可以替换、重新扫描、提交并推送。
5. 发布前必须再次扫描，确认没有遗漏的敏感信息。

推荐使用示例域名 `example.com`、`private.example.com` 或其他明显虚构的占位符，不得使用真实组织名称拼接出的替代域名。
